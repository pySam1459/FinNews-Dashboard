services:
  - type: web
    name: finnews-dashboard
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: OAI_KEY
        sync: false
      - key: DEMO_PASSWORD
        sync: false
